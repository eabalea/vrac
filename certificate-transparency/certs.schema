CREATE TABLE certs (
  entryid INTEGER,
  leafcert VARCHAR2,
  certtype INTEGER,
  issuerid INTEGER
);

CREATE TABLE cacerts (
  certid INTEGER,
  issuerid INTEGER,
  certdata VARCHAR2,
  certhash VARCHAR2
);

